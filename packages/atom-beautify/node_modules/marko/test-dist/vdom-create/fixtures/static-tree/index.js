var expect = require("chai").expect;

module.exports = function (helpers) {
    var link = helpers.vdom.createElement("a", { href: "http://ebay.com" }, null /* key */
    , null /* component */
    , 1 /* childCount */
    ).t("eBay");

    var el = helpers.vdom.createElement("div", { class: "foo", onclick: "doSomething()" }, null /* key */
    , null /* component */
    , 2 /* childCount */
    ).n(link).e("span", null, null /* key */, null /* component */, 0);

    var linkClone = el.au_;

    expect(linkClone).to.not.equal(link);
    expect(link.bi_).to.equal(null);
    expect(link.as_).to.equal(null);

    expect(linkClone.as_.ap_).to.equal("span");
    expect(linkClone.bi_.ap_).to.equal("div");

    return el;
};